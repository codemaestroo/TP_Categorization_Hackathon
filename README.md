# TP_Categorization_Hackathon
Репозиторий для командного хакатона на категоризацию писем


Функционал:

pytests



# Архитектура:

## src - исходники писем отдельно от main тестов

### models.py

    class категорий
    class email
    class ClassificationResult
        категория + причина (почему определили именно сюда) - в логи

    class ProcessingStats
        логи статистика (кол-во входа + кол-во ошибок и верных компиляций)

        логи всего обработанно


### parsers.py

    import re - регулярные выражения

        Часто используемые шаблоны

        \d — любая цифра (0-9).
        \D — любой символ, кроме цифры.
        \w — буква, цифра или подчёркивание.
        \W — любой символ, кроме \w.
        \s — пробельный символ.
        \S — любой непробельный символ.
        ^ — начало строки.
        $ — конец строки.
        . — любой символ, кроме новой строки.
        * — 0 или более повторений.
        + — 1 или более повторений.
        {n,m} — от n до m повторений.


    

tests - проведение тестирований работы проекта

mailbox - распаковка писем, категоризация, логи





Задачи что надо рассписать!!!

1)расписать про нашу логику: файл не читается если не прошел все проверку на "небитость" - тра шаг це нужно (ко всем форматам)

читаемые форматы: txt, json

форматы типа jpeg,png не учитываем . Вложения тоже

2)возможно стоит прописать/повторить регулярные строки из parser

3)закончить описание работы + архитектуры файла parser подобно описанию models (красиво выделять ctrl+shift+R)

4)придумать что делать с инфой внизу писем: условно "файл прикрепил и т д" - мб добавить новую категорию аля "с аргументацией" или протсо "с доп инфой" 

5)провести базовые тесты, а для этого (придумать как будем тестить и как распределять + задать архитектуру)

6)


Основное: (для минималки)

1)Bash-запуск
2)Логи
3)распаковка zip (не уверен, но вроде как тоже надо)
4)pytests

5)main точка входа (приложение или CLI какая нить)


ДОП на 8+ баллов

предлагаю сделать апи и условный список топ требований/тем в полученных письмах, я недавно подобный проект делал. так что вся база у меня есть - то есть беру на себя, но нужно сначала базу доделать

+было бы хорошо всем рублей по 20 скинуть на апи тесты )